<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_amoled">

    <!-- Remote Full Screen Video Surface View Placeholder / Simulated Feed -->
    <RelativeLayout
        android:id="@+id/videoRemoteFeed"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#0A0814">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="[ 4K HD Video Feed Active ]"
            android:textColor="#33FFFFFF"
            android:textSize="14sp" />
    </RelativeLayout>

    <!-- Top Overlay Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:padding="20dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txtVideoCallerName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Alex Rivera"
                android:textColor="@color/text_primary"
                android:textSize="20sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/txtVideoTimer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="05:12 • 1080p 60fps Encrypted"
                android:textColor="@color/emerald_active"
                android:textSize="12sp" />
        </LinearLayout>

        <ImageView
            android:id="@+id/btnFlipCamera"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="@drawable/bg_circle_action"
            android:padding="10dp"
            android:src="@drawable/ic_camera_flip"
            android:contentDescription="Flip Camera" />
    </LinearLayout>

    <!-- Local PiP Self Video Glass Box -->
    <androidx.cardview.widget.CardView
        android:layout_width="110dp"
        android:layout_height="160dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="130dp"
        app:cardBackgroundColor="#1A1828"
        app:cardCornerRadius="16dp"
        app:cardElevation="8dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="You"
                android:textColor="@color/cyan_accent"
                android:textSize="14sp"
                android:textStyle="bold" />
        </RelativeLayout>
    </androidx.cardview.widget.CardView>

    <!-- Bottom Controls Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/btnVideoMute"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/bg_circle_action"
            android:padding="14dp"
            android:src="@drawable/ic_mic"
            android:contentDescription="Mute" />

        <ImageView
            android:id="@+id/btnEndVideoCall"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:layout_marginHorizontal="24dp"
            android:background="@drawable/bg_btn_danger"
            android:padding="20dp"
            android:src="@drawable/ic_call_end"
            android:contentDescription="End Call" />

        <ImageView
            android:id="@+id/btnToggleCam"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/bg_circle_action"
            android:padding="14dp"
            android:src="@drawable/ic_video"
            android:contentDescription="Camera Toggle" />

    </LinearLayout>

</RelativeLayout>
